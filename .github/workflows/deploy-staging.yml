# Deploy do submodule api-platform-common → ambiente staging
# Preferencial: deploy via parent api-community (atualiza todos os submodules).
# Este workflow existe para hotfixes só neste módulo.
# Secrets devem estar configurados NESTE repositório (ou org secrets com acesso).
name: Deploy Staging
on:
  push:
    branches: [staging]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Deploy common → staging
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.DEV_HOST }}
          username: ${{ secrets.DEV_USER }}
          password: ${{ secrets.DEV_PASS }}
          script: |
            set -e
            export PATH="/usr/local/bin:/usr/bin:/bin:$PATH"
            MODULE="/var/www/api-community/modules/controleonline/common"
            cd "$MODULE"
            git fetch origin
            if git show-ref --verify --quiet refs/heads/staging; then
              git checkout staging
            else
              git checkout -B staging origin/staging
            fi
            git reset --hard origin/staging
            git clean -ffd
            cd "$(git rev-parse --show-toplevel 2>/dev/null || echo /var/www/api-community)"
            if [ -f bin/console ]; then
              php bin/console cache:clear --no-interaction || true
            fi
            echo "api-platform-common deployed: $(cd $MODULE && git rev-parse --short HEAD) (staging)"
